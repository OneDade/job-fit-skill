# Contributing

Use Node.js 22.12+. Run `npm ci`, create a focused branch, add tests for behavior and run `npm run check`. Use synthetic/redacted fixtures only. Do not add job scraping, application submission, billing, cloud storage, unsupported resume claims, or provider credentials. Changes to schemas require an explicit compatibility decision and schema version review. Preserve `UPSTREAM.md` mappings when adapting upstream workflow text.
